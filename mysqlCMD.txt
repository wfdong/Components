1.
create table users(ID VARCHAR(30) PRIMARY KEY, NAME VARCHAR(30) NOT NULL, PASSWD VARCHAR(30) NOT NULL, PHONE VARCHAR(30), EMAIL VARCHAR(30), REG_DATE TIMESTAMP, UPDATE_DATE TIMESTAMP);

insert into users(ID, NAME, PASSWD, PHONE, EMAIL, REG_DATE, UPDATE_DATE) values('0000000001', 'admin', 'admin', null, null, now(), now());